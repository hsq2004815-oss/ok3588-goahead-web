#
#   route.txt - Route and authorization configuration
#
#   Schema
#       route uri=URI protocol=PROTOCOL methods=METHODS handler=HANDLER redirect=STATUS@URI \
#           extensions=EXTENSIONS abilities=ABILITIES
#
#   Routes may require authentication and that users possess certain abilities.
#   The abilities, extensions, methods and redirect keywords use comma separated tokens to express a set of
#       required options, or use "|" separated tokens for a set of alternative options. This implements AND/OR.
#   The protocol keyword may be set to http or https. The redirect status may be "*" to match all HTTP status codes.
#   Multiple redirect fields are permissible.
#
#   Examples:
#
#   Universally redirect http to https for secure communications
#       route uri=/ protocol=http redirect=*@https handler=redirect
#
#   Form based login pattern.
#   route uri=/pub/
#   route uri=/action/login methods=POST handler=action redirect=200@/ redirect=401@/pub/login.html
#   route uri=/action/logout methods=POST handler=action redirect=200@/pub/login.html
#   route uri=/ auth=form handler=continue redirect=401@/pub/login.html
#
#   Sample basic or digest authentication for user "joshua"
#       route uri=/auth/basic/ auth=basic abilities=manage
#       route uri=/auth/digest/ auth=digest abilities=manage
#
#   Eanable the PUT or DELETE methods (only) for the BIT_GOAHEAD_PUT_DIR directory
#       route uri=/put/ methods=PUT|DELETE
#
#   Standard routes
#
# route uri=/cgi-bin dir=cgi-bin handler=cgi

route uri=/action/uploadTest handler=action
route uri=/action/mylogin handler=action redirect=200@index.html redirect=401@index.html
route uri=/action/register handler=action
route uri=/action/test handler=action
route uri=/action/buzzer handler=action
route uri=/action/led handler=action

# 添加图片处理相关API路由
route uri=/action/processImage handler=action
route uri=/action/startProcessing handler=action
route uri=/action/stopProcessing handler=action
route uri=/action/getProgress handler=action
route uri=/action/getImageList handler=action
route uri=/action/clearAllServerImages handler=action

# 添加模型选择相关API路由
route uri=/action/setModelPath handler=action
route uri=/action/getAvailableModels handler=action
route uri=/action/deleteModel handler=action
route uri=/action/importFile handler=action

# 添加视频处理相关API路由
route uri=/action/uploadVideo handler=action
route uri=/action/processVideo handler=action
route uri=/action/getVideoProgress handler=action
route uri=/action/getVideoList handler=action
route uri=/action/clearAllVideos handler=action
route uri=/action/setVideoModel handler=action
route uri=/action/getAvailableVideoModels handler=action
route uri=/action/uploadVideoChunk handler=action
route uri=/action/mergeVideoChunks handler=action
route uri=/action/processVideoFrame handler=action

# 添加设备状态API路由
route uri=/action/overview handler=action
route uri=/action/syslog handler=action

route uri=/action/exampleAction handler=action

route uri=/jst extensions=jst,asp,html handler=jst

# route uri=/ methods=OPTIONS|TRACE handler=options
# route uri=/goform handler=action

#
#   Catch-all route without authentication for all other URIs
#
route uri=/

# 修改认证配置为正确的格式
route uri=/action/setModelPath auth=basic handler=action
route uri=/action/getAvailableModels auth=basic handler=action

# 添加WebSocket路由
# route uri=/ws/realtime_video handler=websocket
